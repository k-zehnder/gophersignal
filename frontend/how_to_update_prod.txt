# Install dependencies, build, and export the Next.js application
npm install && npm run build && npm run export

# The output of the build and export commands will appear here
# ... [build and export process output]

# Remove the existing files in the web server's root directory
rm -rf /var/www/gophersignal.com/html/*

# Copy the newly built static files to the web server's root directory
cp -r out/* /var/www/gophersignal.com/html/

# Restart the Nginx service to apply changes
sudo service nginx restart
